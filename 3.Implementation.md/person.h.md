# HEADER FILE 
## person.h 
#ifndef person.h 

#define person.h

char* name(char b[]);

char* letter(char b[]); 

char* num(char b[]); 

char* avai(char b[]); 

char* log(char b[]); 

char* up(char b[]); 

char* log1(char b[]); 

char* up1(char b[]); 

char* log2(char b[]); 

char* up2(char b[]);

#endif
